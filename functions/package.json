{
  "name": "campo-dei-fiori-functions",
  "description": "Cloud Functions per notifiche push FCM",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "test": "node --test test/"
  },
  "engines": { "node": "22" },
  "main": "index.js",
  "dependencies": {
    "@anthropic-ai/sdk": "0.122.0",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.1.0"
  },
  "private": true
}
